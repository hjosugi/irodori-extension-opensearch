[package]
name = "irodori-extension-opensearch"
version = "0.1.0"
edition = "2021"
license = "MIT OR 0BSD"
description = "Irodori Table native connector extension."
publish = false

[lib]
name = "irodori_extension_opensearch"
crate-type = ["cdylib", "rlib"]


[dev-dependencies]
serde_json = "1"

